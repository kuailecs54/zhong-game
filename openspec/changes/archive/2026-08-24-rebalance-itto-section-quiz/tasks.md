## 1. 题目模型与生成规则

- [x] 1.1 在数据类型中增加 `ITTOQuizConfig`、ITTO 分区类型和不可变题目方案类型，并保持非 ITTO 关卡配置向后兼容
- [x] 1.2 先编写纯出题函数的失败测试，覆盖同屏两个不同分区、正确项数量上限、真实项不足时降级和候选项去重
- [x] 1.3 实现核心项优先、普通真实项补足的正确项抽样，使 1.2 测试通过
- [x] 1.4 先编写干扰项失败测试，覆盖同知识领域优先、同过程组与全局逐层补足、排除当前过程全部真实项以及候选池不足降级
- [x] 1.5 实现近邻干扰项生成与候选项洗牌，使 1.4 测试通过

## 2. 关卡难度配置

- [x] 2.1 更新 `public/data/levels.json` 的五个 ITTO 关卡：每题 2 个分区、每区 3 个干扰项，正确项按 1/1/2/3/3 递进，时间按 25/25/30/35/35 秒递进
- [x] 2.2 增加或更新关卡数据测试，验证所有 ITTO 关卡都包含合法 `ittoQuiz` 配置，且非 ITTO 关卡无需该配置
- [x] 2.3 运行 `npm run build`，验证 `levels.json` 修改未破坏 49 过程矩阵约束、类型检查和生产构建

## 3. 本轮题目方案缓存

- [x] 3.1 在 `GameView` 的 ITTO 初始化流程中按本关过程池一次性生成 `processId -> ITTOQuestionPlan` 缓存
- [x] 3.2 调整当前 ITTO 题派生逻辑，使普通出题和复习重现均从缓存读取同一题目方案
- [x] 3.3 调整重新开始流程，在重置统一引擎前重新生成 ITTO 题目方案；暂停、继续和同轮复习不得重建方案
- [x] 3.4 增加可验证复习稳定性的测试：同一过程在本轮多次读取时分区、正确项、干扰项和顺序一致，新一轮可生成新方案

## 4. ITTO 双分区作答界面

- [x] 4.1 调整 `ITTOQuiz` 入参，使组件接收已生成的双分区方案和完整 ITTO，不再在组件内部构造全局干扰池或随机出题
- [x] 4.2 仅渲染方案中的两个分区，并在每个分区明确显示“请选择 N 项”
- [x] 4.3 调整提交判定：仅比较本题抽取项，两个分区均集合完全匹配才上报答对；多选、错选或漏选任一项均上报答错
- [x] 4.4 保留答错复盘停留、键盘提交和完整知识卡；确保批改标记只作用于本题候选项，未抽查真实项不被标错
- [x] 4.5 删除被新出题模块取代的组件内随机函数与 `globalPool` 聚合逻辑，清理仅由本变更产生的未使用代码

## 5. 引擎与统计回归

- [x] 5.1 增加统一引擎回归测试，验证一个过程的两个分区仍只计 1 道总题和 1 次正确/错误判定
- [x] 5.2 验证答错或超时只扣 1 条生命、只加入 1 个复习队列项，答对复习题后按现有规则移除
- [x] 5.3 验证 `userStore` 的 ITTO 掌握度仍按过程每次判定记录一次，localStorage 数据结构不变

## 6. 完整验证

- [x] 6.1 运行 `npm test`，确认题目生成、关卡配置、复习稳定性和引擎回归测试全部通过
- [x] 6.2 运行 `npm run build`，确认 Vue TypeScript 检查与 Vite 生产构建通过
- [x] 6.3 使用 Playwright 在桌面端验证前期、中期、后期 ITTO 关卡分别呈现 1+3、2+3、最多 3+3 的每区候选结构，并显示正确的选择数量提示和倒计时
- [x] 6.4 使用 Playwright 在移动端竖屏及触摸横屏验证双分区选项可换行、页面可滚动、提交按钮可达，HUD 与题面不重叠
- [x] 6.5 手动或通过浏览器自动化答错一题并等待其进入复习队列，确认原题分区、选项与顺序保持不变，知识卡仍展示完整 ITTO
